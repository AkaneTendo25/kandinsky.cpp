#include "name_conversion.hpp"

// Implementation is entirely in the header (inline functions)
// This .cpp exists to satisfy the CMake source list and ensure the
// header compiles correctly as a translation unit.
